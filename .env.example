# Copy to .env in the project root (or src/.env) and fill in values — never commit real keys.

# Required for live Claude replies and integration tests
ANTHROPIC_API_KEY=

# Optional overrides
# CLAUDE_MODEL=claude-sonnet-4-20250514
# LOG_LEVEL=INFO
# PORT=8000
