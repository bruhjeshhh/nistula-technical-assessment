# Copy to `.env` in the repository root (next to this file) or to `src/.env`.
# Fill in values locally — never commit real keys.

# Required for live Claude replies and integration tests
ANTHROPIC_API_KEY=

# Optional overrides
# CLAUDE_MODEL=claude-sonnet-4-20250514
# LOG_LEVEL=INFO
# PORT=8000
